CREATE DATABASE DWD_Project;

CREATE TABLE Players (ID INT(250) NOT NULL, Player_Mapping VARCHAR(10000), Player_Name VARCHAR(10000), Club VARCHAR(10000), Club_Changed VARCHAR(100), PRIMARY KEY (ID));

CREATE TABLE Articles_Arsenal (AID INT(250) NOT NULL, Persons VARCHAR(10000), Source VARCHAR(10000), Validity INT(100), PRIMARY KEY (AID));

CREATE TABLE Articles_Liverpool (AID INT(250) NOT NULL, Persons VARCHAR(10000), Source VARCHAR(10000), Validity INT(100), PRIMARY KEY (AID));

CREATE TABLE Articles_City (AID INT(250) NOT NULL, Persons VARCHAR(10000), Source VARCHAR(10000), Validity INT(100), PRIMARY KEY (AID));

CREATE TABLE Articles_United (AID INT(250) NOT NULL, Persons VARCHAR(10000), Source VARCHAR(10000), Validity INT(100), PRIMARY KEY (AID));